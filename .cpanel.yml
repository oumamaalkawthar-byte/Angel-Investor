# Template for the `deploy` branch's .cpanel.yml — NOT used on `main`.
# The deploy script (scripts/deploy-dist.sh) copies this into the deploy
# branch automatically on every run.
---
deployment:
  tasks:
    - export DEPLOYPATH=/home/akuedu/public_html/new.angelinvestor.pk/
    - /bin/cp -R * $DEPLOYPATH
