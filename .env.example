# Keep unset (or "false") everywhere except the real production deploy.
# When set to "true", the site becomes indexable by search engines and
# robots.txt should be updated to remove the blanket Disallow.
PUBLIC_ALLOW_INDEXING=false
